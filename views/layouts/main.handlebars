<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tech Blog</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>
  <div class="d-flex flex-column">
    <header class="d-flex p-3 justify-content-between container-fluid">
      <h1 class="">
        <a href="/">The Tech Blog</a>
      </h1>
      <nav class="d-flex flex-row align-items-center">
        <h4><a href="/" class="mx-2">Home</a></h4>
        <h4><a href="/dashboard" class="mx-2">Dashboard</a></h4>
        <!-- Conditionally render login or logout links -->
        {{#if logged_in}}
        <h4><a id="logout" class="mx-2">logout</a></h4>
        {{else}}
        <h4><a href="/login" class="mx-2">login</a></h4>
        {{/if}}
      </nav>
    </header>
    
    <main class="container mt-5">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>
    <footer>
      
    </footer>
  </div>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>
